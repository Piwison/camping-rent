import BookingPageClient from "@/components/booking/BookingPageClient";

export const metadata = {
  title: "Book — Basecamp & Co.",
};

export default function BookingPage() {
  return <BookingPageClient />;
}
